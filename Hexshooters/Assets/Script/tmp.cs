using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tmp : MonoBehaviour {
	/*
	// Use this for initialization
	public GameObject player;

	public GameObject[] ProjectileList;

	public GameObject currentProjectile;


	public GameObject mainProjectile;
	public ParticleSystem mainParticleSystem;
	public TrailRenderer mainTrail;

	public float speed = .1f;

	private Vector3 velocity;
	public float distance;
	public float time;
	//public ParticleSystem[] mainParticleSystem;

	private float timeActive;
	void Start () {

	}

	// Update is called once per frame
	void Update () {
		if(Input.GetKeyUp(KeyCode.Space))
		{

			currentProjectile.transform.position = player.transform.position;
			currentProjectile.SetActive (true);
		}

		if (timeActive >= time) {
			mainProjectile.SetActive (false);
			mainTrail.Clear ();
		} else {
			mainProjectile.transform.position += velocity;
			timeActive += Time.deltaTime;
		}


	}

	void OnCollisionEnter(Collision col)
	{
		Debug.Log ("working");
		if (col.gameObject.tag == "collide") {
			mainProjectile.SetActive (false);

		}

	}

	void ActivateParticle(GameObject player)
	{


	}*/
}
